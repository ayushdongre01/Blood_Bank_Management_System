<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blood Donation Certificate</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        .certificate-container {
            max-width: 800px;
            margin: 50px auto;
            background-color: white;
            border: 15px solid #dc3545;
            padding: 30px;
            position: relative;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .certificate-header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #dc3545;
            padding-bottom: 20px;
        }
        .certificate-title {
            font-size: 36px;
            color: #dc3545;
            margin-bottom: 10px;
            font-weight: bold;
        }
        .certificate-subtitle {
            font-size: 22px;
            color: #495057;
        }
        .certificate-content {
            text-align: center;
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 30px;
        }
        .donor-name {
            font-size: 32px;
            font-weight: bold;
            color: #343a40;
            margin: 20px 0;
            font-family: 'Times New Roman', serif;
        }
        .certificate-details {
            margin: 40px 0;
        }
        .certificate-details p {
            margin-bottom: 10px;
        }
        .certificate-footer {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
        }
        .signature {
            text-align: center;
            width: 40%;
        }
        .signature-line {
            border-top: 2px solid #000;
            margin-bottom: 10px;
        }
        .certificate-seal {
            position: absolute;
            bottom: 30px;
            right: 30px;
            width: 120px;
            height: 120px;
            background-image: url('/images/blood-drop.png');
            background-size: contain;
            background-repeat: no-repeat;
            opacity: 0.7;
        }
        .blood-drop {
            color: #dc3545;
            font-size: 24px;
            margin-right: 10px;
        }
        .print-button {
            position: fixed;
            top: 20px;
            right: 20px;
        }
        .certificate-id {
            font-size: 12px;
            color: #6c757d;
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
        }
        @media print {
            body {
                background-color: white;
            }
            .certificate-container {
                margin: 0;
                border: 10px solid #dc3545;
                box-shadow: none;
            }
            .print-button {
                display: none;
            }
        }
    </style>
</head>
<body>
    <button class="btn btn-primary print-button" onclick="window.print()">
        <i class="fas fa-print"></i> Print Certificate
    </button>

    <div class="certificate-container">
        <div class="certificate-header">
            <h1 class="certificate-title">
                <i class="fas fa-tint blood-drop"></i> Certificate of Blood Donation
            </h1>
            <p class="certificate-subtitle">This is to certify that</p>
        </div>

        <div class="certificate-content">
            <h2 class="donor-name"><%= donor.name %></h2>
            <p>has voluntarily donated blood on <strong><%= new Date(donation.donation_date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></strong></p>
            <div class="certificate-details">
                <p><strong>Blood Type:</strong> <%= donor.blood_type %></p>
                <p><strong>Donation ID:</strong> <%= donation.donation_id %></p>
                <p><strong>Volume Donated:</strong> <%= donation.volume_ml %> ml</p>
                <p><strong>Donation Center:</strong> BloodBank Center</p>
            </div>
            <p>This selfless act of kindness has the potential to save up to 3 lives. We express our sincere gratitude for your generosity and commitment to helping others.</p>
        </div>

        <div class="certificate-footer">
            <div class="signature">
                <div class="signature-line"></div>
                <p>Medical Director</p>
            </div>
            <div class="signature">
                <div class="signature-line"></div>
                <p>Blood Bank Administrator</p>
            </div>
        </div>

        <div class="certificate-id">Certificate ID: BD-<%= donation.donation_id %>-<%= new Date().getFullYear() %></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 