<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blood Donation Certificate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: white;
            font-family: 'Arial', sans-serif;
        }
        .certificate {
            border: 2px solid #dc3545;
            padding: 20px;
            margin: 20px auto;
            max-width: 800px;
            position: relative;
            background-color: white;
        }
        .certificate-header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 1px solid #dc3545;
            padding-bottom: 20px;
        }
        .certificate-title {
            color: #dc3545;
            font-size: 36px;
            font-weight: bold;
        }
        .recipient-name {
            color: #0d6efd;
            font-size: 40px;
            font-weight: bold;
            margin: 20px 0;
        }
        .donation-details {
            margin: 30px 0;
        }
        .certificate-footer {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
        }
        .signature-line {
            border-top: 1px solid #333;
            width: 200px;
            margin-top: 10px;
        }
        .certificate-seal {
            position: absolute;
            bottom: 40px;
            right: 40px;
            opacity: 0.2;
            font-size: 100px;
            color: #dc3545;
        }
        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 100px;
            color: rgba(220, 53, 69, 0.05);
            white-space: nowrap;
            z-index: -1;
        }
        @media print {
            .no-print {
                display: none;
            }
            body {
                margin: 0;
                padding: 0;
            }
            .certificate {
                border: 2px solid #dc3545;
                box-shadow: none;
                width: 100%;
                height: 100%;
                max-width: none;
                margin: 0;
            }
        }
    </style>
</head>
<body onload="window.print()">
    <div class="container mt-5">
        <div class="no-print mb-3 text-center">
            <button onclick="window.print()" class="btn btn-danger">
                <i class="fas fa-print me-2"></i> Print Certificate
            </button>
            <a href="/donors/donation/request/<%= donation.donation_id %>" class="btn btn-secondary ms-2">
                <i class="fas fa-arrow-left me-2"></i> Back
            </a>
        </div>
        
        <div class="certificate">
            <div class="watermark">BLOOD DONATION</div>
            
            <div class="certificate-header">
                <div class="certificate-title">CERTIFICATE OF APPRECIATION</div>
                <div>FOR BLOOD DONATION</div>
            </div>
            
            <div class="text-center">
                <p class="mb-0">This certificate is presented to</p>
                <div class="recipient-name"><%= donor.name %></div>
                <p>For the selfless act of donating blood</p>
                
                <div class="donation-details">
                    <p>Donation Date: <strong><%= new Date(donation.appointment_date).toLocaleDateString() %></strong></p>
                    <p>Blood Type: <strong><%= donor.blood_type %></strong></p>
                    <p>Volume: <strong><%= donation.volume_ml %> ml</strong></p>
                    <p>Donation ID: <strong>BDC-<%= donation.donation_id %></strong></p>
                </div>
                
                <p class="mb-0">Your generosity helps save lives and brings hope to those in need.</p>
                <p>Thank you for making a difference!</p>
            </div>
            
            <div class="certificate-footer">
                <div>
                    <div class="signature-line"></div>
                    <p>Medical Director</p>
                </div>
                <div>
                    <div class="signature-line"></div>
                    <p>Donation Center</p>
                </div>
            </div>
            
            <div class="certificate-seal">
                <i class="fas fa-certificate"></i>
            </div>
        </div>
    </div>
</body>
</html> 